<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.jack.coursework4_activitytracker.MainActivity">

    <Button
        android:text="Start Tracking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/startTrackingButton"
        android:layout_below="@+id/textView2"
        android:layout_alignParentStart="true"
        android:layout_marginTop="28dp"
        android:onClick="startTracking" />

    <Button
        android:text="Load From DB"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/startTrackingButton"
        android:layout_toEndOf="@+id/startTrackingButton"
        android:layout_marginStart="15dp"
        android:id="@+id/loadButton"
        android:onClick="loadFromDB" />


    <TextView
        android:inputType="textMultiLine"
        android:text="DB Content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:id="@+id/dbContentView"
        android:layout_below="@+id/startTrackingButton"
        android:layout_alignParentStart="true" />
</RelativeLayout>
